<section class="archive-container">
  <div class="archive-header">
    <h1 class="archive-page-title">文章列表</h1>
    <p class="archive-subtitle">共有 <%= page.posts.length %> 篇</p>
  </div>

  <!-- 标签筛选器 -->
  <%- partial('_partial/tag-filter') %>

  <div class="archive-list">
    <% var lastYear = null; %>
    <% page.posts.each(function(post) { %>
      <% var currentYear = date(post.date, 'YYYY'); %>
      <% var postTags = post.tags.map(function(tag) { return tag.name; }).join(','); %>
      <% if (currentYear !== lastYear) { %>
        <% lastYear = currentYear; %>
        <h2 class="archive-year"><%= currentYear %></h2>
      <% } %>

      <article class="archive-article" data-tags="<%= postTags %>">
        <div class="archive-article-inner">
          <time datetime="<%= date(post.date, 'YYYY-MM-DD') %>" class="archive-article-date">
            <%= date(post.date, 'MM-DD') %>
          </time>
          <h2 class="archive-article-title">
            <a href="<%= url_for(post.path) %>"><%= post.title %></a>
          </h2>
        </div>
      </article>
    <% }); %>
  </div>

  <%- partial('_partial/pagination') %>
</section>
